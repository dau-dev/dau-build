# @package model

_target_: dau_build.build_steps.BuildShellProjectTask
output_root: ???
script: build_mm_job.tcl
vivado: vivado
manifest_name: dau-shell
source_paths: []
metadata: {}
# the composed platform group (platform=platforms/<vendor>/<board>); when
# set, execute=true refuses placeholder boards
platform: ${oc.select:platform,null}
execute: false
